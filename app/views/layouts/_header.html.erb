<nav class="navbar navbar-default" role="navigation">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
	
  </div>

  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse navbar-ex1-collapse">
    <ul class="nav navbar-nav">
        <li><h2 class="pull-left"><%= link_to "Foot-clan.co", root_path %></h2></li>
        <li><%= link_to "Upload", new_shoe_path %></li>
        <% if signed_in? %>
      	  <li class="pull-right"><%= form_tag(search_path, method: "get")  do %>
        <%= text_field_tag :search, params[:search], placeholder: "Search" %>
        <%= submit_tag "Search" %>
        <% end %></li>
        <% end %>
        <li class="pull-right"><a href="mailto:footclanco@gmail.com"> Contact Us</a></li>

        <% if signed_in? %>
    		<li class="pull-right"><%= link_to 'Log out', session_path(current_user), method: "delete" %></li>
    	<% else %>
    		<li class="pull-right"><%= link_to "Register", register_path %></li>
    		<li class="pull-right"><%= link_to "Log in", login_path %></li>
    	<% end %>
      

    </ul>
    <ul class="nav navbar-nav navbar-right">
      
    </ul>
  </div><!-- /.navbar-collapse -->
</nav>